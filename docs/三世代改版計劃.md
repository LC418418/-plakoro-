# 三世代改版計劃（關都 + 城都 + 豐緣）

由「一個關都八館」擴成「三個世代各八館 + 魔鬼模式打通 24 館」嘅可行性研究
同分階段交接文件。

**點用呢份文件**：每個階段開一個**新嘅 Claude session**（唔好接住舊嘅），
第一句就打「睇 `docs/三世代改版計劃.md`，做階段 X」。
（八館改版嗰次證實咗：新 session 平均每次呼叫嘅 context 少一半以上，
同樣工作量平大約一半。呢次個檔更大，慳得更多，見第六節。）

---

## 一、一句話結論

**做得到，冇技術死位**，但唔係一個 session 做得完嘅嘢。

最重要嘅發現有三個：

1. **招式唔使人手寫。** `subType()` / `genPlayable()`（`index.html:1602-1794`）
   係「種子式生成器」—— 加一行圖鑑資料，佢自動出一套招式同骰組，
   同一隻永遠出同一套。加 235 隻新寶可夢嘅**招式成本接近零**。
2. **屬性唔使加。** 遊戲用緊 10 個自訂屬性（草炎水雷超闘悪鋼空無），
   官方 18 個屬性早就映射咗落去（冰→水、龍→空、蟲→草、毒→悪、
   地面→闘、岩石→無、幽靈→悪）。第二、三代冇引入新屬性，
   `TYPES` 一個字都唔使改。
3. **真正嘅成本喺三度**：圖（+437KB base64）、平衡（三個世代 × 八館要逐個量）、
   同排行榜嗰堆歷史包袱（`bestSnapshot` 三重防守嗰段，撞過兩次）。

**預計**：7 個階段、大約 **11-14 個 Claude session**，
你自己落手嘅時間主要係「校中文名」同「Firebase Console 貼規則」。

---

## 二、改完之後會變成點（你講嗰四點）

| # | 你要嘅 | 落到 code 度即係 |
|---|---|---|
| 1 | 加第二、三代寶可夢 + 新技能 | `DEX_RAW` 由 151 行加到 386 行；`DEXSPR` 加 235 張圖；招式池 `PMV`／`MV_T` 加新模板 |
| 2 | 三個世代獨立挑戰，各自八館 + 最終章 | 抽一個 `REGION` 層出嚟，`GYMS`／`ELITE4`／`CHAMPION`／`STARTERS`／`TYPE_FOCUS` 全部變成逐世代 |
| 3 | 魔鬼模式 = 一次過打 24 館 + 最終 boss，六隻由三代名人堂各揀 2 隻 | `ACTS` 由常數變 run-scoped；新一條 `newGauntletRun()`；新一條難度曲線 |
| 4 | 榜顯示完成幾多個世代／魔鬼，徽章唔重複計 | `board` 加 `gens`／`gauntlet`／`badgeMask` 三個欄位，排序改，**`database.rules.json` 要改埋再去 Console 貼** |

---

## 三、可行性逐項評估

### 3.1 加寶可夢資料（第二代 100 隻、第三代 135 隻）　**易**

現況（`DEX_RAW`，`index.html:1411`）：

```js
const DEX_RAW = [
[1,'妙蛙種子','草',1],[2,'妙蛙草','草',2],[3,'妙蛙花','草',3],
```

四個欄位：編號、名、屬性、進化階段（1/2/3，4 = 神獸），第五個可選係弱點覆寫。
加 235 行就搞掂，加上 `EVO`（`index.html:1511`）嘅進化鏈。

**唔好人手打字。** 寫一個 `tools/gendex.py`，由 PokeAPI 嘅
`pokemon-species` 攞官方繁中名（`names[zh-Hant]`）、屬性、進化鏈，
用下面張映射表出 code。人手打 235 行 = 燒好多 token 兼一定有錯。

**屬性映射表（18 → 10）**，由現有關都資料反推出嚟，唔係我砌嘅：

| 官方 | 遊戲 | 憑咩 |
|---|---|---|
| 一般 | 無 | 拉達 19/20 |
| 火／水／草／電／超能力 | 炎／水／草／雷／超 | 直接對應 |
| 格鬥 | 闘 | 希巴「四天王・鬥」`type:'闘'` |
| 飛行 | 空 | 波波 16-18 |
| 毒 | 悪 | 阿柏蛇 23 |
| 地面 | 闘 | 坂木「地面道館」`type:'闘'` |
| 岩石 | 無 | 小剛「岩石道館」`type:'無'` |
| 蟲 | 草 | 綠毛蟲 10-12 |
| 幽靈 | 悪 | 菊子「四天王・靈」`type:'悪'` |
| 冰 | 水 | 勘菜「四天王・冰」`type:'水'` |
| 龍 | 空 | 渡「四天王・龍」`type:'空'` |
| 鋼／惡 | 鋼／悪 | 直接對應 |

⚠ **屬性池會大變，平衡要重量。** 而家非神獸嘅隻數係
水 31・草 24・闘 19・悪 17・空 12・無 12・炎 11・超 11・雷 6・**鋼 2**。
加咗二三代之後鋼會由 2 隻升到大約 14 隻（大鋼蛇、盔甲鳥、巨鉗螳螂、
可可多拉線、鐵啞鈴線……），雷由 6 升到大約 15 隻。
即係 `TYPE_FOCUS`（`index.html:3539`）嗰行**一定要重新校**，
而且鋼系終於夠開一個道館（城都第 6 館阿蜜）。

### 3.2 圖（pixel art）　**中等，但係整個計劃嘅樽頸**

> ⚠ 呢節係**階段 0 做之前**嘅研究。階段 0 已經做咗，
> 真實數字（同埋我當時漏咗嘅 `MAPBG` 252KB）見第五節階段 0。
> 行號亦都係舊嘅 —— 拆檔之後 `index.html` 得返 359KB。

**當時實測**：`DEXSPR` 係**一行** JSON，151 隻
40×40 PNG base64，共 **281KB**，平均每隻 1,861 個 base64 字元。
成個 `index.html` 而家 **1.83MB**，入面 **1.22MB 係圖**：

| 位置 | 內容 | 大細 |
|---|---|---|
| `index.html:1795` | `SCENES` 戰鬥背景 | **745KB** |
| `index.html:1290` | `DEXSPR` 151 隻精靈 | **281KB** |
| `index.html:191` | 主畫面背景 | **178KB** |
| `index.html:1289` | `FACE_IMG` 骰面 | 13KB |

加 235 隻 → **+437KB** → `index.html` 變 **2.27MB**。

#### 實測（Playwright + Chromium，模擬網速，2026-08）

造咗個 386 隻嘅版本，同拆咗檔嘅版本，實際量過：

| | raw | **gzip（真係落線嗰個數）** |
|---|---|---|
| 而家（151 隻） | 1,789KB | **1,206KB** |
| 加二三代（386 隻） | 2,214KB | **1,516KB** |
| 拆檔後嘅 `index.html`（當時只拆 3 舊） | 772KB | **446KB** |

冷啟動（由撳開到玩得）：

| 版本 | WiFi 50M | 4G 20M | 4G 差 5M | 地鐵 1.5M |
|---|---|---|---|---|
| 而家（151 隻） | 0.5s | 0.7s | 2.1s | **6.9s** |
| 加二三代（386 隻） | 0.4s | 0.8s | 2.7s | **8.6s** |
| 拆檔後・**第二次開機**（當時只拆 3 舊） | 0.1s | 0.3s | 0.9s | **2.8s** |

JS 記憶體：151 隻 5.1MB → 386 隻 5.5MB。**記憶體唔係問題**
（base64 喺 V8 度係 one-byte string，慳位）。

**誠實嘅結論**：淨係「加二三代」呢一步，代價係好網幾乎零分別、
爛網慢 0.6 秒、地鐵慢 1.7 秒。**唔算災難。**

真正嘅問題係另外兩樣，而且**而家已經存在**，唔係加二三代先有：

1. **每次開機都重新落同一批圖。** `sw.js` 對 `index.html` 係 network-first，
   而圖就住喺 `index.html` 入面 —— 即係每次開機都重新落嗰 1.2MB 圖，
   就算一個 pixel 都冇改過。一日開十次 = 12MB（改版後 15MB）。
   拆檔之後圖有自己嘅版本化檔名，永久 cache，**第二次開機由 8.6 秒
   跌到 2.8 秒**（地鐵）、2.6 秒跌到 0.9 秒（4G 差區）。
2. ⚠ **`sw.js` 個 `fetch()` 冇 timeout。**（階段 0 已經修好） 佢淨係有 `.catch()` ——
   即係「完全冇網」先會 fallback 去 cache。但「連得到但慢到爬」
   （地鐵隧道、電梯、酒樓地庫）呢種情況佢**唔會 fallback**，
   會一路等到瀏覽器自己放棄，玩家眼前係白畫面。
   呢個係現存嘅真 bug，五行 code 修得好，唔使等大改版。

**圖源調查**（你叫我搵嗰樣）：

| 來源 | 尺寸 | 齊唔齊 | 評價 |
|---|---|---|---|
| [PokeAPI/sprites](https://github.com/PokeAPI/sprites) `versions/generation-iii/firered-leafgreen` | 64×64 | ~~1-386 全齊~~ ❌ **只有 153 張** | ⚠ **呢一行係錯嘅**，階段 1 逐個 HTTP 試過先發現：152-385 全部 404。改用同一代嘅 `emerald`（385/386 齊，322 由 `ruby-sapphire` 補）—— 見階段 1 |
| PokeAPI `versions/generation-ii/crystal` | **40×40** | 只有 1-251 | 尺寸同現有嘅一模一樣（現有 151 張好可能就係由呢度嚟），但冇第三代 |
| [smogon/sprites](https://github.com/smogon/sprites)（Pokémon Showdown） | gen3 64×64／gen5 96×96 | 齊，仲有動畫 | gen5 質素最高但風格同現有嘅唔夾；動畫版體積大好多 |
| [msikma/pokesprite](https://github.com/msikma/pokesprite) | 68×56 box sprite | 齊 | Q 版盒仔圖，同現有戰鬥圖風格唔同 |
| [Pokémon Database](https://pokemondb.net/sprites) | 各代都有 | 齊 | 查圖好用，但要逐張撳，唔啱批量 |

**版權**：所有 sprite 都係 Nintendo／Game Freak／The Pokémon Company 嘅財產
（Showdown 個 repo 明文咁講）。呢個遊戲主頁腳已經寫住「非官方同好製作」、
只喺同事之間玩、冇收錢 —— 加多兩代**唔改變性質**。照做，但唔好放上
app store 或者賣廣告。

### 3.3 三個世代獨立挑戰　**中等偏難（最重嗰個階段）**

要抽一個 `REGION` 層出嚟：

```js
const REGIONS = [
  { id:0, n:'關都', gyms:GYMS_KANTO, e4:E4_KANTO, champ:CH_KANTO,
    starters:[1,4,7],     dex:[1,151],   focus:FOCUS_KANTO },
  { id:1, n:'城都', gyms:GYMS_JOHTO, ... starters:[152,155,158], dex:[152,251] },
  { id:2, n:'豐緣', gyms:GYMS_HOENN, ... starters:[252,255,258], dex:[252,386] },
];
```

改動範圍（grep 數過）：`ACTS` 出現 **48 次**、`GYMS` **6 次**、`DEX[` **19 次**。
`GYMS` 少係好消息 —— 但 `ACTS` 48 次要留返階段 5 先郁（見下）。

**要跟住改嘅位**：

- `run.gen` 落存檔 → `SAVE_VER` 由 4 升到 **5**
  （⚠ `SAVE_MIN_VER` 唔好一齊升，唔係人哋玩緊嗰局會俾剷走）
- 名人堂由 3 格變成**每世代 3 格**：`plakoro.hof.<gen>.<i>`、
  雲端 `users/<uid>/teams/<gen>/<i>` —— **要寫 migration**
  （舊 `teams/0-2` 當成 `teams/0/0-2`，即係關都）
- `tools/simlib.js` 加 `gen` 參數（佢已經冇寫死道館，只係用 `ACTS`／`ELITE4`，
  改動細）

**難度「同第一世代相約」**：`TIER` 四個端點 + `ACT_TUNE` 兩行照用同一套，
唔使每代一套。**要重新校嘅只有 `TYPE_FOCUS`**（池大細變咗）同每代嘅
凸位／凹位（好似關都第 4 章莉佳因為草系易剋而凸咗嗰種）。

⚠ **三個世代都會撞「兩館同屬性」**（同關都撞過嗰個一樣）：

| 世代 | 撞邊兩館 |
|---|---|
| 城都 | 第 1 館阿速（飛行→空）vs 第 8 館小椿（龍→空） |
| 豐緣 | 第 1 館千里（岩石→無）vs 第 5 館千歲（一般→無） |

同關都「小剛 vs 坂木」一樣，跟返上次嘅決定 B（就咁擺住，靠 `TYPE_FOCUS`
分開強度）就得。

⚠ **城都四天王同關都撞名**：阿桔（Koga，關都第 5 館館主 → 城都四天王）、
希巴／志清（Bruno，兩代都係四天王）、渡／阿渡（Lance，關都四天王 →
城都冠軍）。原著本來就係咁 —— 呢個係**特色唔係 bug**，
但你要決定顯示嗰陣點寫（見第四節決定 3）。

### 3.4 魔鬼模式改成 24 館通天塔　**難（風險最高）**

呢個係最大嘅改動，因為 `ACTS` 由「編譯時常數」變成「一局嘅屬性」。
48 處引用要逐個睇，而且好多喺排行榜嗰邊（`ACTS+1` = 通關）。

**設計上嘅新嘢**：

- 隊伍唔再係「三選一抽卡」，係由三個世代嘅名人堂**各揀 2 隻**（硬性）
- 一局 24 章 × 6 關 ≈ **96 場以上**（而家八館制係 32 場）—— 一局玩幾個鐘
- 25 個 boss（24 館主 + 最終）要一條新曲線
- 通關率預計會低到量唔到（後半樣本本身就薄）

⚠ **同現有嘅設計原則有衝突**，要你拍板（見第四節決定 1）：
`index.html:4445` 個註解明文寫住「名人堂讀返出嚟淨係打得對戰，
接唔返落挑戰模式。呢個限制係故意嘅：一但接得返，就等於 save/load 番晒嚟，
永久死亡就冇曬意思」。魔鬼模式用名人堂隊伍就係破咗呢條規。

### 3.5 排行榜改版　**中等，但係最易靜靜雞出事嗰個**

現況（`index.html:6080-6168`）已經有三重防守，係撞過兩次真事之後補返嘅：
`FB.rogue === null` 唔寫榜、`plakoro.best.<uid>` 本機鏡像取大、
`wins > 0` 就一定顯示通關。呢三層**全部都要跟住改**。

**新排序**（你要嘅）：魔鬼完成 → 完成幾多個世代最終 boss → 徽章數 → …

**新欄位**：

| 欄位 | 型 | 做咩 |
|---|---|---|
| `gauntlet` | number 0/1 | 魔鬼通關咗未 |
| `gens` | number 0-7 | 三個 bit，邊幾個世代打爆咗最終 boss |
| `badgeMask` | number 0-16777215 | **24 個 bit**，邊幾個道館拎過徽章 |

⚠ **「重新挑戰獲得嘅徽章唔重複計算」呢個要求，逼住要用 bitmask。**
而家個 `badges` 係一個數字（`Math.max(...)`），重打第 3 館攞多次
一樣會計。改成 24-bit mask 之後 `popcount(mask)` 就係真正嘅徽章數，
天然唔會重複。

⚠⚠ **`database.rules.json` 個 `board` 節點有 `$other: false`。**
新欄位唔喺 rules 度列明，**成個寫入會俾人拒絕，而且唔會報錯，
個名就係唔更新**。而且 `badges` 上限而家寫住 `<= 8`，要改到 `<= 24`。
改完 **要你自己去 Firebase Console 貼**，唔會自動生效。

### 3.6 新技能　**易，但會推翻平衡**

`MV_T`（`index.html:3268`）10 個模板、`PMV`（`index.html:1642`）玩家招式池、
`TYPE_FLAVOR` 負責換名。加新模板本身好易，但**每加一個都要重跑
`measure.mjs` 對返八個數**。所以擺喺平衡校完之後做，唔好撈埋一齊。

---

## 四、⚠ 五件事要你先決定

呢五樣唔決定就做唔落。

### 決定 1：魔鬼模式用名人堂隊伍，等於破咗「唔准 load」嘅規矩　✅ 老細揀 A

現有設計係「永久死亡」：名人堂只可以打對戰，接唔返落挑戰。
你要嘅魔鬼模式係直接用名人堂六隻去打 24 館。

- **A（建議・已採用）**：魔鬼模式係**獨立終局挑戰**，入面**唔可以捉新寶可夢、
  唔會進化／特訓**（六隻本身已經係練成品），但照有遺物／商店／道具袋。
  即係「用你練成嘅隊伍去考試」，唔係「load 咗個檔繼續練」。
  永久死亡嘅精神喺三個世代嗰邊照樣守住。
- **B**：照舊會成長。咁魔鬼模式就變咗最抵玩嘅練功模式，
  三個世代反而冇人打。
- **C**：魔鬼模式輸咗就要重新用**新一次**通關嘅隊伍先入到（消耗名人堂）。
  最狠，但同事之間可能玩到嬲。

### 決定 2：加咗二三代之後，關都嗰局會唔會撞到二三代寶可夢？　✅ 老細揀 A

- **A（建議・已採用）**：唔會。關都局只抽 1-151，城都局只抽 152-251，
  豐緣局只抽 252-386。乾淨、好平衡、亦都最貼「獨立挑戰」個講法。
- **B**：城都局可以撞到關都嘅（原著金銀就係咁）。池大好多，
  但平衡要重新量，而且「玩多啲新寶可夢」呢個目的會被溝淡。

### 決定 3：撞名點顯示

阿桔（關都第 5 館 → 城都四天王）、希巴／志清（Bruno）、渡（關都四天王 →
城都冠軍）。

- **A（建議）**：照原著，重複就重複。玩開嘅人會覺得「係喎，佢升咗職」。
- **B**：城都嗰邊改個名避開。

### 決定 4：要唔要順手換走現有 151 張圖，統一做火紅葉綠風格　✅ 老細揀 A

火紅葉綠（`generation-iii/firered-leafgreen`）有齊 1-386，
三個世代美術**完全一致**。但現有 151 張玩開嘅圖會變樣。

- **A（建議・已採用）**：全部換成火紅葉綠。你自己都講第一代係「火紅葉綠版關都」，
  正好對應。而且淨係要跑一次腳本。
- **B**：關都保留現狀，城都用金銀水晶（40×40 原生），
  豐緣用火紅葉綠縮圖。省事但三代風格唔統一。

### 決定 5：`index.html` 唔再係單一檔案（階段 0）　✅ 老細話跟建議做

要拆走嗰 1.22MB 圖做外部檔，壞處係 `CLAUDE.md` 講嘅「單檔網頁遊戲」
呢個特性冇咗。

**⚠ 建議嘅理由唔係「2.27MB 太大」** —— 實測過（見 3.2），
加二三代本身淨係慢 0.6-1.7 秒，唔算災難。**真正嘅理由係三個**：

1. **而家每次開機都重新落同一批冇改過嘅圖。** 拆咗之後圖永久 cache，
   第二次開機由 8.6 秒跌到 2.8 秒（地鐵）。呢個係**改善現狀**，
   唔係「補救加二三代嘅代價」。
2. **`index.html` 由 1,789KB 跌到 772KB** → 之後六個階段
   Claude 讀／grep 個檔都平一半，係整份計劃入面最抵嘅一筆 usage 投資。
3. 順手可以喺同一個階段修埋 `sw.js` 冇 timeout 嗰個現存 bug。

---

## 五、七個階段

> 每個階段一個新 session。每個階段做完**都應該可以出版**（`sw.js` 加版本號），
> 唔會出現「做到一半玩家玩唔到」。

### 階段 0：拆走圖，起返個地基　　✅ **做咗**

**做咩**：

1. `SCENES`／`DEXSPR`／主背景／`FACE_IMG` 由 `index.html` 搬去
   `assets/scenes-v1.js`、`assets/dexspr-v1.js` 等等。`sw.js` 對呢批
   用 **cache-first**（檔名帶版本號，改咗就係新檔名），`index.html`
   維持 network-first。
2. 順手修 `sw.js:32` 冇 timeout 嗰個 bug：`fetch()` 包一個
   3-5 秒嘅 race，過咗就用 cache 開住先，網返嚟先靜靜更新。

**實測到嘅回報**（見 3.2）：

- `index.html` **1,789KB → 772KB**（gzip 1,206KB → 446KB）
- **第二次開機**：地鐵 8.6s → **2.8s**、4G 差區 2.6s → **0.9s**
- 之後每個階段 Claude grep／read 個檔都平一半 → **直接慳你 usage**
- 純搬運，冇邏輯改動，風險最低，最啱做第一步

**驗收**：三個 test 全綠、GitHub Pages 上載完離線照玩、iOS 加到主畫面照玩、
飛行模式開得到、**慢網（開 devtools throttle 到 1.5Mbps）唔會白畫面**。

**風險**：低。唯一要小心係 GitHub Pages 嘅相對路徑同 PWA scope。

#### 做完之後嘅實測

拆走五舊 base64（連我一開始漏咗嘅 `MAPBG` 252KB），共 **1,430KB**。
背景圖／場景圖／地圖底圖還原做真 `.webp`（唔使 base64，慳埋嗰 33% 冗餘），
`DEXSPR` 保持 base64 但搬做 `assets/dexspr-v1.js`，
`FACE_IMG`（12KB）留喺 `index.html`（唔值得為佢多開一個 request）。

| | 拆檔前 | 拆檔後 |
|---|---|---|
| `index.html` | 1,789KB（gzip 1,206KB） | **359KB（gzip 130KB）** |
| `assets/` 18 個檔 | — | 1,142KB（永久 cache） |

冷啟動（由撳開到玩得）：

| | WiFi 50M | 4G 20M | 4G 差 5M | 地鐵 1.5M |
|---|---|---|---|---|
| 拆檔前 | 0.4s | 0.6s | 2.1s | **6.9s** |
| 拆檔後・第一次開 | 0.1s | 0.2s | 0.7s | **2.2s** |
| 拆檔後・第二次開機 | 0.1s | 0.1s | 0.3s | **1.0s** |

⚠ 連**第一次開**都快咗三倍，唔止第二次 —— 因為玩得到（`__plk.ready`）
只需要 `index.html` + `dexspr`，戰鬥背景同地圖底圖係 CSS，
第一次用到先落，唔阻住開機。

`sw.js` 同時改咗兩樣：`assets/` 同遊戲本體分開兩個 cache
（升遊戲版本號唔會連圖一齊清），同埋個 `fetch` 加咗 4 秒 timeout
（慢網唔會再白畫面等到死）。

---

### 階段 1：圖 + 資料管線　　✅ **做咗**

**做咗啲乜**：

1. `tools/gendex.py` —— 由 PokeAPI 出 `DEX_RAW` 152-386 同 `EVO`。
   ⚠ `pokeapi.co` 俾開發環境嘅 egress proxy 擋咗（403），
   改用官方靜態鏡像 `PokeAPI/api-data` 嘅 raw.githubusercontent。
2. `tools/fetchdex.py` —— 落 386 張圖，出三個 `assets/dexspr-genN-v1.js`。
3. `DEX_GENS`／`ACTIVE_GEN`／`genPool()` —— 世代分界，`ACTIVE_GEN` 釘死喺 0。
4. 圖鑑改成四頁（三個世代 + 官方卡表），撳一隻睇招式同骰組。
5. `tools/test-dex.mjs` 18 項新測試。

**驗收結果**：四個 test 全綠（18 + 53 + 36 + 抗性）。開機速度冇退步 ——
地鐵網（1.5M）由 3.2s 快到 **2.9s**，因為開機路徑得關都嗰 203KB，
仲細過舊嗰個 276KB 嘅單一檔。

#### ⚠ 兩件同計劃書寫嘅唔同

**(1) 火紅葉綠嘅圖唔齊。** 第 3.2 節話 `firered-leafgreen`「1-386 全齊」，
**實測係假嘅**：個資料夾只有 **153 張**（1-151 加 216、386），
152-385 全部 404。改用同一代嘅 **`emerald`**（385/386 齊），
呆火駝（322）由 `ruby-sapphire` 補。兩個都係第三代 GBA 嘅圖，
決定 4 要嘅「三個世代美術完全一致」照樣做到。
**如果你想關都嗰 151 隻用返火紅葉綠**（佢有齊 1-151），
改 `fetchdex.py` 個 `FALLBACK` 就得，一行嘢。

**(2) 唔縮圖。** 計劃書話縮到 40×40 減色。做嘅時候發現冇必要 ——
原檔本身係 16 色 index PNG，一張得 ~700 bytes，**仲細過**舊嗰批 40×40
（~1,400 bytes）。而且縮到 40×40 係 0.625 倍非整數比例，pixel art 一定糊；
裁走透明邊又會搞亂相對大細（畫面用緊 `object-fit:contain`，
裁咗之後綠毛蟲同烈空坐會顯示到一樣大）。照用原生 64×64，清晰又細。

#### 反推返出嚟嘅兩條規則（`gendex.py check` 驗過）

用現有關都 151 隻做標準答案，逐項對返生成規則：**進化鏈 0/151 錯**、
屬性錯 1 隻（暴鯉龍，人手調嘅）、階級錯 5 隻（拉達、皮卡丘、臭泥、伊布、
快龍，都係人手評嘅強度）、名 2 隻（舊譯）。

- **屬性**唔係「揀第一個」，係跟一個優先次序：磁怪（電/鋼）出「鋼」、
  迷唇姐（冰/超）出「超」、呆呆獸（水/超）出「水」。
- **階級 `s`** 唔係鏈嘅深度：單階算 3（大蔥鴨嗰批「單階強者」）、
  兩階鏈 1→3（跳過 2）、三階先至 1→2→3、官方標住 baby 嘅算 1，
  而且**只計同一個世代入面嗰段鏈**（大鋼蛇喺城都池自己就係單階強者）。

#### ⚠ 留低咗俾階段 2 嘅嘢（已經做咗）

**19 條跨世代進化冇加**（`gendex.py apply` 會列返出嚟）：
大岩蛇→大鋼蛇、飛天螳螂→巨鉗螳螂、伊布→太陽／月亮伊布、
皮丘→皮卡丘、迷唇娃→迷唇姐 …… 加咗即係關都局會進化出城都寶可夢，
撞正決定 2（三個池唔溝埋）。

**副作用**（階段 2 處理咗）：城都豐緣有幾隻 baby 喺自己世代入面
`s=1`（＝抽卡池會當佢係基礎精靈出）但**永遠進化唔到**，
因為佢哋嘅進化型喺上一個世代。
⚠ **實際係 8 隻唔係 6 隻** —— 呢度漏咗皮寶寶(173)、寶寶丁(174)：
皮丘 172、皮寶寶 173、寶寶丁 174、迷唇娃 238、電擊怪 239、鴨嘴寶寶 240、
露力麗 298、小果然 360。
老細揀咗 A：**開局三選一唔會出佢哋**（`DEAD_END_BASIC`／`canDraft()`），
野外照撞得到、捉得到、圖鑑照有。用計出嚟嘅規則 `s===1 && !EVO[n]`
唔用硬名單，所以 `gendex.py` 改咗進化鏈會自動跟。

**中文名**：235 隻全部係 PokeAPI 出嘅官方繁中譯名，我掃過一次冇發現錯。
你想再核一次就開圖鑑撳「城都」「豐緣」兩頁慢慢碌。

---

### 階段 2：世代化架構（最重嗰個）　　✅ **做咗**

**做咗啲乜**：

1. `REGIONS`（`index.html` ~3741）—— 三個世代各自一份
   `gyms`／`e4`／`champ`／`starters`／`focus`。`setGen(g)` 一次過綁曬
   `GYMS`／`ELITE4`／`CHAMPION`／`STARTERS`／`TYPE_FOCUS`（呢五個由
   `const` 變咗 `let`），所以原本十幾處引用**一個字都唔使改**。
2. 城都、豐緣嘅八館 + 四天王 + 冠軍隊伍。
3. `run.gen` 落存檔，`SAVE_VER` 4 → **5**（`SAVE_MIN_VER` 照留 4）。
   `serialize`／`deserialize` 嘅 `gymOrder` 改成跟**存檔嗰個世代**攞。
4. 名人堂由 3 格變成**每代 3 格**（`plakoro.hof.<gen>.<i>` /
   `users/<uid>/teams/<gen>/<i>`）+ 本機同雲端 migration。
5. 開新一局先揀世代（`openGenPick`）；`rgStart(slot, gen)` 會 `setGen`
   同 `await loadDexSpr`。
6. `tools/simlib.js` 嘅 `draftParty()` 跟世代行、`measure(diff, N, gen)`；
   `measure.mjs` 加第三個參數、`sweep.mjs` 加 `GEN=` 環境變數。
7. `tools/test-dex.mjs` 由 18 項加到 **41 項**（逐個世代跑一次）。

**驗收結果**：四個 test 全綠（41 + 53 + 36 + 抗性）。
關都嘅通關率同改版前一致（400 局：69/66/58/57/62/68/68/53），冇回歸。

#### ⚠ 三件同計劃書寫嘅唔同

**(1) `ACTS` 48 次引用一次都唔使郁。** 計劃書驚嘅係 `ACTS`，但實際上
真正要世代化嘅係 `GYMS`／`ELITE4`／`CHAMPION`／`STARTERS`／`TYPE_FOCUS`
呢五個，加埋 grep 出嚟得十幾處。用「`let` + `setGen()` 重新綁定」
而唔係「逐處改成 `REGIONS[gen].gyms`」，改動面細好多。
`ACTS` 要到階段 5（24 館）先真係要郁。

**(2) baby 係 8 隻唔係 6 隻。** 階段 1 嗰段漏咗皮寶寶(173)、寶寶丁(174)。
所以用咗計出嚟嘅規則 `s===1 && !EVO[n]`（`DEAD_END_BASIC`）唔用硬名單。
老細揀 A：開局三選一唔會出佢哋，野外照撞得到、捉得到、圖鑑照有。

**(3) 有兩個世代開唔到某幾館嘅屬性焦點。** 池太細：
城都嘅「空」得 3 隻（咕咕、貓頭夜鷹、刺龍王）但阿速同小椿兩館都係空；
豐緣嘅「闘」得 3 隻（藤樹）、「雷」得 4 隻（鐵旋）。
呢幾格 `focus` 暫時開 0（唔係成場撞返同一隻），階段 3 再算。
順手發現**關都本來就有個重複**：椰蛋樹(103) 同時喺莉佳同青綠隊入面 ——
莉佳嗰格係魔鬼第 5 館起先出而佢係第 4 館，即係永遠出唔到，
改咗做蛋蛋(102)，零風險。

#### 城都八館（實際落咗嘅隊伍）

| # | 館主 | 遊戲屬性 | 隊伍 |
|---|---|---|---|
| 1 | 阿速 | 空 | 咕咕・貓頭夜鷹・波克基古・信使鳥 |
| 2 | 阿蕊 | 草 | 芭瓢蟲・圓絲蛛・安瓢蟲・壺壺 |
| 3 | 小茜 | 無 | 尾立・大奶罐・驚角鹿・大尾立 |
| 4 | 松葉 | 悪 | 夢妖・黑暗鴉・月亮伊布・未知圖騰 |
| 5 | 阿蓮 | 闘 | 頓甲・天蠍・小小象・沙基拉斯 |
| 6 | 阿蜜 | 鋼 | 大鋼蛇・佛烈托斯・巨鉗螳螂・多邊獸Ⅱ |
| 7 | 柳伯 | 水 | 長毛豬・太陽珊瑚・小山豬・章魚桶 |
| 8 | 小椿 | 空 | 刺龍王・班基拉斯・巨翅飛魚・赫拉克羅斯 |

四天王：一樹（超）・阿桔（悪）・志清（闘）・梨花（悪）／冠軍：阿渡（空）

⚠ 原著有一半館主帶關都寶可夢（阿速帶波波、阿蜜帶磁怪、阿渡帶快龍），
決定 2 唔准溝，所以全部換成同代最接近嗰隻，隊頭兩隻盡量守返原著。
阿渡嗰隊尤其係硬換（城都得刺龍王一隻龍，而佢係小椿嘅王牌）。

#### 豐緣八館

館主隊伍全部係原著嗰批（豐緣自成一代，唔使代換）：
千里（無）・藤樹（闘）・鐵旋（雷）・亞莎（炎）・千歲（無）・娜琪（空）・
小楓＆小南（超）・亞當（水）；四天王 花月・芙蓉・婓伊・源治／冠軍 達雅（鋼）

#### ⚠ 留低咗俾階段 3 嘅嘢

**城都豐緣嘅通關率完全未校過**（階段 2 講明唔理平衡）。400 局實測：

| | 第1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 館 |
|---|---|---|---|---|---|---|---|---|
| 關都（基準） | 69 | 66 | 58 | 57 | 62 | 68 | 68 | 53 |
| 城都 | 79 | 84 | **28** | 66 | 71 | 62 | 81 | 59 |
| 豐緣 | 59 | 75 | 60 | 61 | **31** | 70 | 64 | 56 |

城都第 3 館（小茜・無）同豐緣第 5 館（千歲・無）兩個大凹位都係「一般系道館」
——`focus` 開到 0.55-0.70 但兩代嘅「無」系入面有幾隻好硬（大奶罐、幸福蛋、
請假王），值得由呢度入手。⚠ 樣本 400 局，後面幾館係雜訊，要 3000 局起跳先睇得。

### 階段 3：三個世代嘅平衡　　✅ **做咗**

**做咗咩**：加咗第三層平衡旋鈕 `GEN_TUNE`（逐個世代嘅逐章修正，
數字喺 `REGIONS[g].tune`，由 `setGen()` 綁），用佢校返城都同豐緣。
**關都一個數都冇郁** —— `TIER`／`ACT_TUNE`／`DIFFS` 全部原封不動。

#### 估錯咗嘅嘢：唔關 `TYPE_FOCUS` 事

上面階段 2 留低嗰段估「兩個凹位都係一般系道館，`focus` 開到 0.55-0.70
但嗰兩代嘅無系有幾隻好硬」。**跑咗六組 focus 證實估錯**：拉到 0
（成章一隻無系雜魚都冇）城都第 3 館 30→33、豐緣第 5 館 32→30，
即係凹位根本唔喺雜魚度。

於是喺模擬器度加咗兩欄診斷（`measure.mjs` / `sweep.mjs` 而家都會出）：

| | 館主場勝率 | 未見館主就死 |
|---|---|---|
| 城都第 3 館（小茜・無） | **32%**（鄰章 73-88%） | 10% |
| 豐緣第 5 館（千歲・無） | **29%**（鄰章 54-77%） | 19% |

真正原因喺 `payCost`：**「無」係萬用能量**，所以「無」系嘅招式（cost 全部
係「無」）**任何骰面都找得掂**。其他屬性嘅敵人成日擲唔夠色而放空，
「無」系永遠唔會 —— 一個「無」系館主等於一個「唔會失手」嘅館主，
喺中段大約值 **-25 到 -30 點**通關率。關都個「無」係第 1 館（敵人數值最低），
所以一直冇人發現。

⚠ **之後擺「無」系道館落中後段之前要先諗定**（階段 5 嘅 24 館通天塔會撞返）。
而家係喺 `GEN_TUNE` 嗰格補返，**冇改規則** —— 改規則（例如「無」系敵人
唔准當萬用）會連玩家自己嗰邊一齊改，唔屬於呢個階段。

#### 校完之後嘅數值

| `tune.hp` | 第1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 關都（基準） | 1 | 1 | 1 | 1 | 1 | 1 | 1 | 1 |
| 城都 | 1.12 | 1.22 | **0.78** | 1 | 1 | 1 | 1 | 1 |
| 豐緣 | 0.92 | 1.06 | 1 | 1 | **0.78** | 1 | 1 | 1 |

`tune.dmg` 三個世代都係全 1 —— 傷害狠一倍，血量調得掂就唔好掂佢。
`TYPE_FOCUS` 一個都冇郁（證實咗唔關佢事）。

#### 校完之後嘅數（困難）

| | 第1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 館 | 四天王 |
|---|---|---|---|---|---|---|---|---|---|
| 關都（6000 局，冇郁過） | 69 | 68 | 59 | 58 | 65 | 63 | 68 | 56 | 60% |
| 城都（4000 局・校完） | 65 | 70 | 61 | 57 | 64 | 65 | 64 | 62 | 52% |
| 豐緣（4000 局・校完） | 66 | 67 | 61 | 62 | 63 | 62 | 64 | 58 | 54% |
| 城都・校之前 | 79 | 84 | **30** | 60 | 66 | 65 | 60 | 59 | 48% |
| 豐緣・校之前 | 58 | 74 | 62 | 61 | **32** | 67 | 70 | 65 | 53% |

三個世代而家同一個形狀。後半（第 5-8 館）三個都企喺 56-68 ——
高過 band 但**同關都一致**，即係老細早前決定「刻意保留」嗰個鬆度
（`CLAUDE.md` 講嗰樣），唔係城都豐緣有問題。

**魔鬼：特登唔校，亦都唔好再花時間校。** 老細（2026-08）拍板：
**現有嘅逐個地區魔鬼模式會喺階段 5 取消**，變成一次過打三個地區所有 boss
嗰個 24 館通天塔。即係話而家嘅 `DIFFS[1]` 係一個就嚟拆走嘅嘢 ——
校佢等於校緊一個唔會存在嘅模式。

留低嘅數淨係做參考：城都 10000 局量到 52/45/28/41/49/26/38/38，
大致同關都嗰條線一樣、後半仲緊啲（`GEN_TUNE` 困難嗰邊校完，魔鬼順手食埋）。
**豐緣嘅魔鬼冇量過，亦都唔使量。**

---

### 階段 4：新技能　　✅ **做咗**

**原本嘅計劃**：`PMV` 加新招、`MV_T` 加新模板（例如連續技、反傷、隊友治療）。
每加一個模板重跑一次 `measure.mjs`。擺喺平衡之後做，因為加模板一定會推翻階段 3 嘅數。

#### 加咗五個新模板（玩家同敵人共用同一套 key）

| key | 名（例：草／闘） | 做咩 | 邊個 slot | 敵人有冇 |
|---|---|---|---|---|
| `snipe` | 藤蔓追擊／趁隙追打 | 對手上回合失敗就 +40 傷 | cheap | 有（`mob`） |
| `counter` | 種子反噬／反擊 | 還返對手上一招卡面寫住嘅傷害 | mid | 有（`e4`） |
| `combo` | 連環種子／二連踢 | **擲兩次角色骰**，兩次都加傷 | strong | 有（`elite`／`champ`） |
| `gamble` | 瘋狂植物／地裂 | 上前左 ×2、下後 直接失敗 | heavy | **冇**（見下） |
| `mend` | 芳香治療／鼓舞 | **後備隊友**各回 30 HP | util | 有（`gym`） |

另加 `evade`（迴避）—— 佢本來冇自己個 key，靠推斷跌咗落 `guard`，
即係叫做「尖刺防守」但效果係完全免疫，同真・防守撞名兼撞意思。

**引擎冇點改**：`repeat`／`chain`／`reflect`／`x2`／`fail`／`charRolls`
官方 12 隻卡本來就用緊，一直只係冇畀生成嘅角色用。唯一真係新加嘅 op 係
`partyHeal`（隊友治療）—— 引擎本身淨係識得 `game.p[]` 兩隻上場嘅，
後備隊伍係 `battleStart` 加落 `game.sides` 嘅，所以要 guard 住經典 1v1。

#### 順手掘返出嚟嘅兩個 bug

**(1) `PMV.util` 成組模板由頭到尾係死嘅。** `genPlayable` 生成 7 招之後寫
`moves[6] = 副屬性招` —— 最後嗰格 util 一出世就俾人踩咗。屏障／療愈／迴避／
集氣四個模板**冇一隻寶可夢攞得到**，連「學新招」嘅獎勵都見唔到
（`moveAdds` 拎緊同一條清單）。改成 `push` 之後每隻 8 招。
存檔冇事：`serializeRun` 存嘅係成個招式物件，唔係索引。

**(2)「加咗招但冇人肯揀」。** 揀邊招嘅地方有**四份**一模一樣嘅
「攞 `faces[0]` 入面嗰個 `add`」：`enemyChoose`、`bestFour`、`monPowerScore`、
同 `simlib` 嘅 `playerChoose`。即係除咗純加傷之外乜都當零分 ——
加個「反擊」或者「援護」落去，四邊都當佢冇價值，永遠唔會出現喺任何一副牌，
**而且唔會報錯、測試照過**。而家四邊一律借 `faceEV(mv, opp)`，
`EV_W` 係唯一一份權重表。

順手仲修埋：`mkMove` 唔再自己寫一個描述 switch（借返 `opText`）、
三個畫面唔再淨係畫 `charRolls[0]`（連續技擲兩次，以前得一粒骰畫面）。

#### 平衡：三個世代一齊升咗 6 點，用曲線壓返

新模板落池 + util 出返街之後（6000 局・困難・關都）：

| | 第1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 館 |
|---|---|---|---|---|---|---|---|---|
| 階段 3 嘅基準 | 68 | 68 | 59 | 58 | 67 | 61 | 62 | 60 |
| 加咗新招（未調） | 75 | 74 | 65 | 64 | 68 | 67 | 68 | 66 |

**升幅八章都一樣**＝玩家嘅底盤強咗，唔係邊一章出咗事。
所以修返係郁**曲線**（`TIER` 嘅 `hp0`/`hp1` 一次過加 8%：`mob` 74→80 / 253→273、
`elite` 134→145 / 398→430、`gym` 265→286 / 920→994），
唔係郁 `ACT_TUNE`（`ACT_TUNE` 要留返「逐章凹凸」個意思）。
`GEN_TUNE` 一個數都冇郁 —— 三個世代嘅升幅一樣，唔關世代事。
四天王／冠軍亦都冇跟住加：佢哋自己一條曲線，而佢哋嗰邊本身已經由
54% 跌到 46%（多咗人打得入去，入面嗰批平均弱咗）。

校完（6000 局・困難・三個世代各量一次，括號入面係階段 3 嘅基準）：

| | 第1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 館 | 四天王 |
|---|---|---|---|---|---|---|---|---|---|
| 關都 | 67 (68) | 65 (68) | 59 (59) | 60 (58) | 64 (67) | 64 (61) | 59 (62) | 53 (60) | 42% |
| 城都 | 68 (66) | 64 (71) | 57 (60) | 58 (59) | 65 (63) | 62 (65) | 68 (65) | 60 (66) | 54% |
| 豐緣 | 64 (67) | 66 (68) | 60 (59) | 58 (61) | 61 (63) | 67 (62) | 66 (63) | 64 (58) | 49% |

三個世代全部返返去階段 3 嗰個形狀（逐格差 ±7 之內，大部分 ±3）。

⚠ **有查過係咪「模擬器變咗聰明」而唔係「玩家真係強咗」**：
`faceEV` 換咗之後，用**舊內容 + 新眼鏡**量返一次係 70/70/61/58/63/60/65/57，
即係換眼鏡本身大約值 +1 點（雜訊範圍）。**六點入面差唔多全部係內容。**

#### ⚠ 敵人唔可以有 `x2`

敵人嘅基本傷害本身已經乘咗成局倍率：champ 第 8 章「爆發」係 130 傷，
×2 就係 260-320，而玩家嗰陣平均先得 317 血 —— 一擊清枱。
`tameAdd` 壓得住角色骰嗰下加成，**壓唔住 `x2`**（佢係乘埋基本傷害）。
所以 `gamble` 淨係玩家有，`MV_T` 度冇。`test-moves.mjs` 有一項專門守住。

**新測試**：`tools/test-moves.mjs`（41 項）—— 五種效果真係做到嘢、
十個屬性 × 每個模板都有名、每個角色骰面都有描述、`faceEV` 睇得到新 op、
援護唔會令倒低咗嘅翻生、敵人冇 ×2、新招入得存檔又出得返、
三個畫面都畫齊粒粒角色骰。

---

### 階段 5：魔鬼模式 24 館通天塔　　⏸ **押後（老細 2026-08 叫暫時唔好做）**

> ⏸ **2026-08：老細話「唔搞新魔鬼 mode 住」，先出階段 6、7。**
> 所以而家嘅狀態係：**魔鬼難度封咗**（`DIFFS[1].lock = '待更新'`），
> 開局畫面出個灰掣寫住「待更新」，撳唔到。剩返「困難」揀得。
>
> ⚠ **封咗唔等於剷咗，亦都唔可以剷。** `DIFFS[1]` 要原封不動留住：
> **(1)** 舊存檔嘅 `run.diff===1` 要接得返（`rgResume` 會 `DIFF=run.diff`）——
> 剷咗 `clampDiff` 就會靜靜哋當佢係「困難」，人哋打緊嗰局突然易咗；
> **(2)** 名人堂、排行榜、PvP 快照都記住咗 `diff`，剷咗顯示會由「魔鬼」
> 變成「困難」，等於改咗人哋嘅歷史紀錄。
> `test-features.mjs` 有七項守住呢幾樣。
>
> ⚠ **老細 2026-08 原本嘅拍板：呢個唔係「多一個模式」，係『換咗』個模式。**
> 現有嗰個逐個地區都有嘅魔鬼難度（`DIFFS[1]`）**會取消**，
> 魔鬼變成一次過打三個地區所有 boss 嗰個 24 館通天塔。
> 所以**唔好花時間校現有魔鬼嘅平衡** —— 校緊嘅嘢就嚟拆。
> 拆嗰陣要記住：`DIFFS` 而家係兩級（0 困難 / 1 魔鬼），
> `migrateDiff()`、存檔入面嘅 `run.diff`、名人堂同排行榜嗰邊都有得引用，
> 唔可以淨係喺 `DIFFS` 度剷走一行。

**真係開始做嗰陣要知**：階段 6 已經幫你留咗位 ——
`database.rules.json` 嘅 `board` 已經列明咗 `gauntlet` 呢個欄位（0/1），
即係**唔使再叫老細貼多次規則**；`boardFetch` 嗰個 sort 上面亦都寫低咗
「通天塔做完之後喺最頭加多一格 `gauntletOf`」。

**做咩**：

- `ACTS` 由常數變 run-scoped（48 處引用逐個睇）
- `newGauntletRun()`：三個世代名人堂各揀 2 隻
- 25 個 boss 嘅新曲線（24 館主連住打 + 最終）
- 一局 96+ 場，存檔一定要穩陣（中途閂 app 要接得返）
- 揀隊伍嘅新畫面

**⚠ 決定 1 一定要喺呢個階段之前拍板。**

**驗收**：由頭打到尾唔會斷、中途閂 app 接得返、`measure.mjs` 跑到
（simlib 要加 gauntlet 模式）。

---

### 階段 6：排行榜 + Firebase 規則　　✅ **做咗**

#### 徽章由「一個數」變成「24 個 bit」

舊制個 `badges` 係「最遠嗰一局拎到幾多個」，最多 8。三個世代之後應該係
「三個世代加埋一共拎過幾多個」，最多 24 —— 而且**重打同一個道館唔可以再計**。
單靠一個數做唔到（重打第 3 館一樣會 +1），所以改成 mask：

```
badgeMask  24 個 bit：第 g 個世代第 i 個道館 = bit g*8+i
gens        3 個 bit：邊幾個世代打爆過冠軍（1 關都 2 城都 4 豐緣）
```

`popcount(badgeMask)` 就係真正嘅徽章數，天然唔會重複，
亦都唔會因為換咗個世代玩而跌返落嚟。兩個 mask **永遠只會「或」上去**
（同 `best`/`badges` 取最大值係同一個道理），run 完局嗰個 transaction
入面做，所以兩部機同時完局都唔會撞跌對方嗰啲 bit。

⚠ 加第四個世代就會去到 32 bit（JS 位元運算得 31 bit）——
嗰陣要改用兩個數或者字串，**唔好靜靜哋加落去**。

#### 排序改咗

```
通關咗幾多個世代 → 一共幾多個徽章 → 爬得最遠（道館 → 關數） → 戰力 → 近期
```

⚠ 頭兩個係「三個世代加埋」嘅累計，第三個先係「單一局最遠」——
次序反過嚟嘅話，打爆咗兩個世代嘅人會輸畀一個打到第 8 館嘅人。
通天塔（階段 5）做完之後喺最頭加多一格 `gauntletOf` 就得。

#### 三重防守跟住改咗

- `bestSnapshot` 兩個 mask 都係「雲端 | 本機鏡像 | 呢一局」
- `healBestFromHof()` 由「數吓通關過幾多次」變成**連邊個世代都補返**——
  名人堂每格都記住咗 `gen`，佢係「舊資料重建 `gens`／`badgeMask`」嘅唯一來源；
  通關咗嗰個世代 = 嗰八個徽章一定齊
- 舊資料冇 mask（`badges` 最多 8），所以 `badgesOf()` 要同舊嗰幾個來源取大 ——
  唔係老玩家一開個榜就見到自己徽章跌咗

#### ⚠ 「規則未貼」而家唔會再靜靜雞爆煲

`board` 有 `$other: false`，Console 未貼新規則之前，新欄位
（`badgeMask`／`gens`）同埋 `badges > 8` 會令**成個寫入俾人拒絕**，
唔報錯，個名就係唔更新。呢個坑喺呢個 codebase 撞過兩次，所以加咗兩重防守：

1. **`boardWrite()` 寫失敗會自動退返舊格式再試一次**（掉走新欄位、
   `badges` 夾返 8）—— 未貼規則個榜都仲行得，貼咗之後第一次寫就自動行返新格式，
   唔使做任何嘢。
2. **`test-auth.mjs` 直接對住 `database.rules.json` 檢查**：
   `boardRecord` 寫嘅每個欄位規則都有列明冇（`$other:false` 嗰個坑），
   同埋 `badges`／`badgeMask`／`gens`／`best` 幾個上限夠唔夠大。
   `fbstub` 唔會執行規則，所以呢一項唔係跑遊戲量返嚟，係直接讀條規則。

**新測試**：`test-auth.mjs` 由 36 項加到 **52 項**。重點嗰幾項：
重打同一個世代唔會重複計徽章、打第二個世代唔會令第一個世代嗰八個消失、
舊資料冇 mask 都唔會跌數、排序真係世代數行先、規則未貼會退返舊格式、
`board` 每個欄位規則都列明咗。

**你要落手嘅**：⚠ **去 Firebase Console 貼新規則**
（Console → Realtime Database → 規則 → 全選剷走 → 貼 `database.rules.json` → 發佈）。
唔貼嘅話個榜會行返舊格式 —— 唔會爆，但 `badgeMask`／`gens` 寫唔入，
三個世代嘅累計徽章就顯示唔到。

---

### 階段 7：出版收尾　　✅ **做咗**

`sw.js` 升到 `poketower-v47` → merge 落 `main`（GitHub Pages 派 `main`）→
更新 `CLAUDE.md`（檔案地圖、分區行號、新旋鈕、新踩坑、schema）。

---

## 六、點樣慳 usage（你問嘅重點）

### 6.1 最大嗰筆：階段 0 拆走圖

`index.html` 而家 1.83MB，入面 1.22MB 係 base64。
每次 Claude 要 grep／睇個檔嘅結構，都要喺呢舊嘢中間游水。
`CLAUDE.md` 本身都要用一整節去教「唔好 cat、唔好 grep 到 base64 嗰幾行」。

拆走之後實測剩 **772KB**（量過，唔係估）。**之後六個階段每一個都平** ——
呢個係一次投資、六次回報。

### 6.2 每個階段開新 session

八館改版嗰次量過：新 session 平均每次呼叫嘅 context 少一半以上，
同樣工作量平大約一半。呢份文件就係為咗令新 session 唔使重新摸底。

### 6.3 資料錄入要用腳本，唔好人手打

235 行 `DEX_RAW` + 進化鏈人手打，係幾萬個 token 嘅輸出，而且一定有錯，
錯咗要改又要再燒一次。寫個 60 行嘅 Python 由 PokeAPI 生成，**平幾十倍**。
同一個道理套喺 235 張圖度。

### 6.4 平衡放最後，而且用模擬器

模擬器跑幾多局**完全唔影響 usage**（返嚟都係同一舊細 JSON）。
所以：

- 結構（階段 2）同平衡（階段 3）**分開做**，唔好一路寫一路調
- 一次 sweep 塞多幾組設定去試，唔好一次試兩三組
- 局數 3000 起跳，唔好慳

### 6.5 每個階段獨立可出版

咁樣就算你中途想停低唔做落去，之前做嘅嘢一樣落到玩家手。
唔會出現「投資咗一半，冇嘢見到」。

### 6.6 唔好一個 session 做兩個階段

尤其係階段 2（世代化）同階段 5（24 館）—— 兩個都係大範圍改動，
撈埋一齊 context 會爆，出錯之後要重讀成個檔，反而更貴。

---

## 七、成本總結

| 階段 | 內容 | Session | 你落手 | 風險 |
|---|---|---|---|---|
| 0 | 拆走圖、起地基 ✅ | 1 | 15 分鐘驗收 | 低 |
| 1 | 圖 + 資料管線（386 隻）✅ | **1** | 名已經掃過，撳開圖鑑核一核就得 | 低 |
| 2 | 世代化架構 ✅ | **1** | 譯名照計劃書用咗 | **中高** |
| 3 | 三代平衡 ✅ | 1-2 | 1 小時睇數 | 中 |
| 4 | 新技能 ✅ | **1** | — | 中 |
| 5 | 魔鬼 24 館 ⏸ 押後 | 2-3 | 決定 1 要拍板 | **高** |
| 6 | 排行榜 + 規則 ✅ | **1** | **Console 貼規則** | 中（易靜靜雞出事） |
| 7 | 出版收尾 ✅ | 0.5 | 驗收 | 低 |
| | **合計** | **11-14** | | |

**如果要斬件出**：階段 0-4 做完，你已經有「三個世代獨立挑戰、
386 隻寶可夢、新技能」—— 即係你四個要求入面嘅頭兩個，
亦都係「畀玩家玩多啲寶可夢」呢個主要目的。
階段 5-6（魔鬼 24 館 + 新榜）可以之後先做，唔會有半成品留喺度。

---

## 八、風險同陷阱（做之前一定要知）

1. **`board` 加欄位一定要重貼規則。** `$other: false` 會靜靜雞拒絕成個寫入，
   唔報錯，個名就係唔更新。撞過。
2. **排行榜三重防守唔可以拆。** `FB.rogue === null` 唔寫榜、本機鏡像取大、
   `wins > 0` 一定顯示通關 —— 呢三層係真事撞出嚟嘅
   （有人打爆冠軍之後個榜寫返「第 8 道館」）。加世代欄位嗰陣要三層一齊改。
3. **`SAVE_MIN_VER` 唔好隨手升。** 升咗＝人哋玩緊嗰局俾剷走。
4. **`tools/simlib.js` 要跟住遊戲改。** 佢自己重寫咗寶箱／商店／換人嗰幾段。
   遊戲加咗新嘢佢冇跟，量出嚟嘅通關率會偏低而且**唔會報錯**。
5. **平衡對數值極敏感**：魔鬼第 1 章 `dmg` 加 0.01 通關率跌 15 點。
   血量一行 0.02-0.05、傷害 0.01-0.02，兩樣分開試。
6. **後面幾章樣本天生細**：6000 局先得 ~120 局行到第 8 館。跳 ±10 點係雜訊。
   24 館通天塔呢個問題會更加嚴重 —— 可能要 50000 局先有得睇。
7. **加新資源要三個位一齊改**：`ITEMS`／`CHEST_LOOT`／`openShop`，
   加埋 `serializeRun` + `deserializeRun`，同埋 `tools/simlib.js`。
   **加新招式模板同樣要五個位一齊改**（階段 4 學返嚟）：`PMV`／`MV_T`、
   `TYPE_FLAVOR`（**十個屬性**）、`opText`、`EV_W`。漏咗 `EV_W` 最惡 ——
   四個揀招嘅地方當佢零分，即係「加咗招冇人肯揀」，唔會報錯。
8. **GitHub Pages 派 `main`。** 喺 branch 做完記得 merge 返落 `main`
   （撞過：做完冇 merge，玩家一直行緊舊 code）。

---

## 九、下一步

階段 0、1、2、3、4、6、7 做完；階段 5（魔鬼 24 館通天塔）押後咗。
**要接落去做通天塔就開新 session 打「睇 `docs/三世代改版計劃.md`，做階段 5」。**

五個決定全部拍板咗 ✅：
決定 1（A：魔鬼模式唔會成長）、決定 2（A：三個池唔溝埋）、
決定 3（A：撞名照原著保留）、決定 4（A：全部換第三代版嘅圖）、決定 5（拆檔）。
階段 1 掘返出嚟嗰個 baby 問題亦都揀咗 A（開局抽卡池排除）。

**階段 0-4、6、7 做完 = 已經出咗街**：三個世代獨立挑戰、386 隻寶可夢、
新技能、三個世代累計嘅新排行榜。`sw.js` 喺 `poketower-v47`，已經 merge 落 `main`。

**⏸ 剩返階段 5（魔鬼 24 館通天塔）** —— 老細 2026-08 話「唔搞住」，
所以魔鬼難度而家封咗（`DIFFS[1].lock = '待更新'`），開局畫面出個灰掣、撳唔到。

**⚠ 老細仲有一件事未做：去 Firebase Console 貼新規則**
（Console → Realtime Database → 規則 → 全選剷走 → 貼 `database.rules.json` → 發佈）。
唔貼唔會爆（`boardWrite` 會自動退返舊格式），但 `badgeMask`／`gens` 寫唔入，
三個世代嘅累計徽章就顯示唔到。

階段 5 開波要知：

- **`board` 已經留咗 `gauntlet` 呢個欄位**（規則度列明咗，客戶端未寫）——
  即係做通天塔嗰陣**唔使再叫老細貼多次規則**。
  `boardFetch` 個 sort 喺最頭加一格 `gauntletOf` 就得（註解已經寫低）。
- **封咗嘅魔鬼要點處理**：`DIFFS[1]` 原封不動留住（舊存檔／名人堂／排行榜
  都引用緊佢），做完通天塔就係「換走 `DIFFS[1]` 嘅內容」而唔係「加多一行」。
  `test-features.mjs` 嗰七項要跟住改。

- 平衡而家係**三層**（`曲線 × ACT_TUNE × GEN_TUNE`），三個世代嘅**困難**
  校完之後都企喺同一個形狀（見階段 4 嗰張表）。改嘢之後
  **三個世代都要重量一次**（`GEN=0/1/2`），唔係淨係量關都。
- ⚠ **唔好花時間校現有嘅魔鬼**（豐緣魔鬼冇量過亦都唔使量）—— 佢就係
  階段 5 要拆走嗰樣。拆嗰陣記住 `DIFFS` 而家係兩級，`migrateDiff()`、
  存檔嘅 `run.diff`、名人堂同排行榜都有得引用，唔可以淨係剷走一行。
- ⚠ 24 館通天塔會將三個地區嘅道館撈埋一齊 —— 即係**「無」系道館一定會
  落到中後段**（關都個「無」而家喺第 1 館所以一直冇事）。
  `payCost` 當「無」係萬用能量，「無」系館主永遠唔會能量不足，
  一個就值 -25 到 -30 點通關率。排 24 館嘅次序之前要先諗定呢樣。
- ⚠ 加新招式模板要五個位一齊改（階段 4 學返嚟）：`PMV`／`MV_T`、
  `TYPE_FLAVOR`（**十個屬性**）、`opText`、`EV_W`。
  另外新模板嘅 `cost` 唔好寫成全「無」（同上面「無」系嗰個坑一樣）。
